# Как собрать APK - Краткая инструкция

## ✅ Что уже готово:
- ✓ Проект настроен
- ✓ Модель YOLO 8n в формате TFLite находится в `app/src/main/assets/yolov8n.tflite`
- ✓ Gradle wrapper настроен
- ✓ Все исходные файлы на месте

## 🚀 Быстрый старт (3 шага):

### Шаг 1: Установите Java JDK
```bash
sudo apt update
sudo apt install openjdk-17-jdk
```

Или используйте скрипт:
```bash
./INSTALL_DEPS.sh
```

### Шаг 2: Установите Android SDK

**Вариант A (рекомендуется):** Установите Android Studio
- Скачайте: https://developer.android.com/studio
- Установите - SDK будет настроен автоматически

**Вариант B:** Только SDK через командную строку
См. подробные инструкции в `BUILD_INSTRUCTIONS.md`

### Шаг 3: Соберите APK
```bash
./build_apk.sh
```

Или вручную:
```bash
./gradlew assembleDebug
```

## 📦 Результат

После успешной сборки APK будет в:
```
app/build/outputs/apk/debug/app-debug.apk
```

## 📱 Установка на устройство

```bash
# Подключите Android устройство через USB
# Включите "Отладка по USB" в настройках разработчика

adb install app/build/outputs/apk/debug/app-debug.apk
```

## ⚠️ Текущий статус

Для сборки APK требуется:
1. ✅ Java JDK 17+ - **НЕ УСТАНОВЛЕНА** (установите: `sudo apt install openjdk-17-jdk`)
2. ⚠️ Android SDK - **НЕ НАСТРОЕН** (установите Android Studio или SDK)

## 📚 Дополнительная информация

- Подробные инструкции: `BUILD_INSTRUCTIONS.md`
- Быстрый старт приложения: `QUICKSTART.md`
- Общая документация: `README.md`

## 🔧 Альтернатива: Использование Android Studio

Если у вас установлен Android Studio:
1. Откройте проект в Android Studio
2. Дождитесь синхронизации Gradle
3. Build → Build Bundle(s) / APK(s) → Build APK(s)
4. APK будет в `app/build/outputs/apk/debug/`




