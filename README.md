# Android Object Detection App with YOLO 8n

Приложение для Android с распознаванием объектов в реальном времени с использованием модели YOLO 8n.

## Возможности

- 📷 Доступ к камере устройства
- 🔍 Распознавание объектов в реальном времени
- 📦 Использование модели YOLO 8n (YOLOv8 nano)
- 🎯 Отображение bounding boxes с метками и уверенностью

## Требования

- Android Studio Hedgehog или новее
- Android SDK 24 (Android 7.0) или выше
- Устройство с камерой или эмулятор с поддержкой камеры

## Установка

1. Клонируйте репозиторий или откройте проект в Android Studio
2. Добавьте модель YOLO 8n в формате TensorFlow Lite:
   - Скачайте модель `yolov8n.tflite` (можно конвертировать из ONNX или PyTorch)
   - Поместите файл в `app/src/main/assets/yolov8n.tflite`
3. Синхронизируйте проект с Gradle файлами
4. Запустите приложение на устройстве или эмуляторе

## Получение модели YOLO 8n

Модель YOLO 8n нужно конвертировать в формат TensorFlow Lite. Вот несколько способов:

### Способ 1: Использование готовой конвертации
1. Скачайте модель YOLOv8n из официального репозитория Ultralytics
2. Конвертируйте в TFLite используя Python:

```python
from ultralytics import YOLO

# Загрузите модель
model = YOLO('yolov8n.pt')

# Экспортируйте в TFLite
model.export(format='tflite')
```

### Способ 2: Использование онлайн конвертеров
- Используйте онлайн сервисы для конвертации ONNX/PyTorch в TFLite

## Структура проекта

```
app/
├── src/main/
│   ├── java/com/compvision/app/
│   │   ├── MainActivity.kt          # Главная активность с камерой
│   │   ├── YOLODetector.kt          # Детектор объектов YOLO
│   │   └── ObjectDetectionOverlay.kt # Оверлей для отображения результатов
│   ├── res/
│   │   ├── layout/
│   │   │   └── activity_main.xml
│   │   └── values/
│   └── assets/
│       └── yolov8n.tflite           # Модель YOLO (нужно добавить)
└── build.gradle
```

## Использование

1. Запустите приложение
2. Разрешите доступ к камере при первом запуске
3. Наведите камеру на объекты - они будут автоматически распознаваться
4. Результаты отображаются с bounding boxes и метками

## Технические детали

- **CameraX**: Для работы с камерой
- **TensorFlow Lite**: Для запуска модели YOLO
- **Kotlin**: Основной язык разработки
- **ViewBinding**: Для работы с UI

## Примечания

- Модель должна быть в формате TensorFlow Lite (.tflite)
- Размер входного изображения: 640x640 пикселей
- Поддерживается 80 классов объектов из COCO dataset
- Порог уверенности по умолчанию: 0.5
- NMS (Non-Maximum Suppression) порог: 0.4

## Лицензия

Этот проект создан для образовательных целей.





